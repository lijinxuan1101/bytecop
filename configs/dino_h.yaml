# DINOv3 ViT-H+ single-tower training config
backbone: dino_h
epochs: 10
batch_size: 32
lr: 1.0e-4
weight_decay: 0.01
unfreeze_blocks: 4
proj_dim: 512
dropout: 0.1
clean_prob: 0.3          # fraction of training samples left unaugmented
